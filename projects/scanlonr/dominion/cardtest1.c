/*
 * card test 1 (smithy)
 */
#include "dominion.h"
#include "dominion_helpers.h"
#include "rngs.h"
#include "myAssert.h"
#include <stdlib.h>
#include <stdio.h>

int
main()
{
	// testing is done though cardEffect which is assumed to operate correctly
	// for testing smithy we are only concered with local variables
	// state, currentPlayer, handPos, i from cardEffect
	int card = smithy;
	int choice1 = 0;
	int choice2 = 0;
	int choice3 = 0;
	int handPos = 0;
	int bonus = 0;


	// test smithy when player has empty deck

	return 0;
}
